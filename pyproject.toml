[project]
name = "agentflow-proxy"
version = "0.1.0"
description = "Local provider-specific proxy for Claude and Codex with caching, crunching, routing, and logging."
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "httpx>=0.27",
  "pydantic>=2.6",
  "python-dotenv>=1.0",
  "pyyaml>=6.0",
  "websockets>=15.0",
  "zstandard>=0.23",
]

[project.scripts]
agentflow-proxy = "agentflow_proxy.server:main"
agentflow-claude-proxy = "agentflow_proxy.server:main"
agentflow-dashboard = "agentflow_proxy.dashboard:main"

[tool.setuptools]
packages = ["agentflow_proxy"]
